import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#056950',
      dark: '#02322C',
    },
    common: {
      white: '#ffffff',
    },
    grey: {
      300: '#e0e0e0',
    },
  },
});

export default theme;
